import { QueryClientProvider } from '@tanstack/react-query'
import { AppRouter } from './routes'

function App() {
  return (
    // <QueryClientProvider>
      <AppRouter/>
    // </QueryClientProvider>
  )
}

export default App
